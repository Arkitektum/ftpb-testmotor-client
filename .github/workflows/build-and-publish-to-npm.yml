name: Publish Node.js Package

on:
  release:
    types: [created]

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  publish-npm-package:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      # No registry-url here on purpose: it makes setup-node write an .npmrc with
      # //registry.npmjs.org/:_authToken=${NODE_AUTH_TOKEN}, which makes npm think the registry already has auth
      # configured and skip the OIDC exchange that trusted publishing relies on.
      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"

      - name: Enable Corepack and activate Yarn 4
        run: |
          corepack enable
          yarn --version

      # A registry timeout can crash Yarn outright instead of failing one fetch, so retry the whole install rather than trusting Yarn's own retries to recover.
      - name: Install dependencies
        run: |
          for attempt in 1 2 3; do
              yarn install --immutable && exit 0
              echo "::warning::yarn install failed (attempt $attempt/3), retrying in 15s"
              sleep 15
          done
          exit 1

      # The published artifact is the declarations as much as the JavaScript, so the types are checked before it goes out.
      - name: Typecheck
        run: yarn typecheck

      - name: Run tests
        run: yarn test

      - name: Build project
        if: ${{ success() }}
        run: yarn build

      # Trusted publishing needs npm >= 11.5.1 for the OIDC exchange.
      - name: Ensure npm supports trusted publishing
        run: |
          npm install -g npm@latest
          npm --version

      # No NODE_AUTH_TOKEN: npm authenticates through OIDC as the trusted publisher configured on npmjs.com. Even an
      # empty token env var disables that exchange. If this ever fails with ENEEDAUTH, rerun with --loglevel verbose
      # to see the "npm verbose oidc" lines: the trusted publisher fields (owner, repo, workflow filename,
      # environment) are matched exactly and are case-sensitive.
      - name: Publish to npm
        if: ${{ success() }}
        run: npm publish --provenance --access public
